import { createSlice } from '@reduxjs/toolkit';

export const matchingSlice = createSlice({
    name: "matching",
    initialState: ,
    reducers: {
    },
});

export const {} = matchingSlice.actions;
export default matchingSlice.reducer;